# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
env/
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg

# Virtualenv
venv/
.env

# Database (Optional: Keep nba_betting.db if you want to ship data, exclude if local only)
# isportsapi.db is large, maybe exclude? 11MB is fine for git though (limit is 100MB).
# We'll include them for now so the app works out of the box on Streamlit Cloud.
# *.db

# Streamlit
.streamlit/
